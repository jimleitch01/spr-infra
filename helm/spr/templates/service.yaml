apiVersion: v1
kind: Service
metadata:
  name: {{ template "spr.fullname" . }}
  labels:
    app: {{ template "spr.name" . }}
    chart: {{ template "spr.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "spr.name" . }}
    release: {{ .Release.Name }}
